# færdiggør indstillingerne en anden gang, gør config mere overskueligt.

() ensret i3 farve med den fra ghostty farve
	() gør det samme med polybar
	() lav evt. et central colors directory i .config
